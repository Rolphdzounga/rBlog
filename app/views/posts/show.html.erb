<div class="post-single">
	<div class="post-content">
		<h2 class="page-header"><%= @post.title %></h2>
<% if @u %>
<ul class="listing">
	<div class="row ">
		<div class="col-md-4 col-md-offset-4 ">
			<li>Publié par : </li>
			<li align="center"><%= link_to gravatar_for(@u) , usr_path(@u)  %></li>
			<li class="post-title" align="center"><%= link_to @u.username, usr_path(@u) %></li>
			<li align="center"><small><%= pluralize(@u.posts.count, "post") if @u.posts %></small></li>
		</div>
	</div>
</ul>
<%end%>
		<small>Posted by <%= @post.user.name %> <br> On <%= @post.created_at.to_time.strftime('%e %B at %l:%M %p') %>
		</small>
		<%= image_tag @post.image.url , width:'100%'  %>
		<%= raw @post.body %>
	</div>
</div>




<%= render 'partials/comments' %>
