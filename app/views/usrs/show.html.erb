<h3 align="center">Bienvenu sur le profil  de <%= @user.username %></h3>
<div class="row">
	<div class="col-md-4 col-md-offset-4 center">
		<%= gravatar_for @user, size: 150 %>
	</div>
</div>
<h4 align="left">Posts Publiés par <%= @user.username %></h4>

<%= render 'posts/post' , obj: @user.posts %>